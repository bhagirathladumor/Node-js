<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Edit Task</title>

<style>
    body {
        background: #f2f2f2;
        font-family: Arial, sans-serif;
        margin: 0;
    }

    .container {
        max-width: 600px;
        margin: 30px auto;
        background: white;
        padding: 20px;
        border-radius: 6px;
        box-shadow: 0 0 6px rgba(0,0,0,0.1);
    }

    label {
        display: block;
        margin-top: 15px;
        font-weight: bold;
    }

    input, textarea, select {
        width: 100%;
        margin-top: 6px;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 4px;
    }

    .actions {
        margin-top: 20px;
        display: flex;
        justify-content: space-between;
    }

    .btn {
        padding: 10px 18px;
        border-radius: 4px;
        text-decoration: none;
        border: none;
        cursor: pointer;
    }

    .btn-update { background: #007bff; color: white; }
    .btn-delete { background: #dc3545; color: white; }
    .btn-back { color: #007bff; margin-top: 20px; display: inline-block; }
</style>
</head>

<body>

<div class="container">
    <h2>Edit Task</h2>

    <form action="/updateTask" method="POST">

        <input type="hidden" name="id" value="<%= task.id %>">

        <label>Title</label>
        <input type="text" name="title" value="<%= task.title %>" required>

        <label>Description</label>
        <textarea name="task"><%= task.task %></textarea>

        <label>Date</label>
        <input type="date" name="date" value="<%= task.date %>" required>

        <label>Start Time</label>
        <input type="time" name="startTime" value="<%= task.startTime %>" required>

        <label>End Time</label>
        <input type="time" name="endTime" value="<%= task.endTime %>" required>

        <label>Status</label>
        <select name="status">
            <option <%= task.status === "Pending" ? "selected" : "" %>>Pending</option>
            <option <%= task.status === "Completed" ? "selected" : "" %>>Completed</option>
        </select>

        <div class="actions">
            <button class="btn btn-update" type="submit">Update</button>
            <a class="btn btn-delete" href="/deleteTask?id=<%= task.id %>">Delete</a>
        </div>

    </form>

    <a href="/" class="btn-back">← Back</a>
</div>

</body>
</html>
