<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Task Manager</title>
<style>
    body {
        font-family: Arial, sans-serif;
        background: #f2f2f2;
        margin: 0;
        padding: 0;
    }

    .container {
        max-width: 700px;
        margin: 30px auto;
        background: white;
        padding: 20px;
        border-radius: 6px;
        box-shadow: 0 0 6px rgba(0,0,0,0.1);
    }

    h1 {
        margin-bottom: 10px;
    }

    .btn-add {
        display: inline-block;
        background: #007bff;
        padding: 10px 14px;
        color: white;
        text-decoration: none;
        border-radius: 4px;
        margin-bottom: 20px;
    }

    .task {
        background: #fafafa;
        padding: 15px;
        margin-bottom: 10px;
        border-radius: 4px;
        border-left: 4px solid #007bff;
    }

    .task-title {
        font-weight: bold;
        font-size: 16px;
    }

    .task-time {
        color: #555;
        margin: 5px 0;
        font-size: 14px;
    }

    .actions a {
        margin-right: 12px;
        text-decoration: none;
        color: #007bff;
    }

    .no-task {
        text-align: center;
        padding: 30px 0;
        color: #777;
    }
</style>
</head>

<body>

<div class="container">
    <h1>Tasks</h1>

    <a class="btn-add" href="/newTask">+ Add Task</a>

    <% if (task.length === 0) { %>
        <div class="no-task">No tasks found. Add a task to begin.</div>
    <% } %>

    <% task.map((task)=>{ %>
        <div class="task">
            <div class="task-title"><%= task.title %></div>
            <div><%= task.task %></div>
            <div class="task-time"><%= task.date %> • <%= task.startTime %> - <%= task.endTime %></div>

            <div class="actions">
                <a href="/editTask?id=<%= task.id %>">Edit</a>
                <a href="/deleteTask?id=<%= task.id %>">Delete</a>
            </div>
        </div>
    <% }) %>

</div>

</body>
</html>
