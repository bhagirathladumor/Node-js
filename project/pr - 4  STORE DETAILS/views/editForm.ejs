<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Booking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body{
            background-color: #e0e7ff !important; /* Indigo-100 Blue */
        }
    </style>
</head>
<body class="">

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">Edit Booking</h4>
                    </div>
                    <div class="card-body">
                        <form action="/update/<%= booking.id %>" method="POST">
                            
                            <div class="mb-3">
                                <input type="hidden" name="id" value="<%= booking.id %>" hidden>
                                <label class="form-label">Guest Name</label>
                                <input type="text" value="<%= booking.name %>" name="name" class="form-control" value="<%= booking.name %>" required>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Room Type</label>
                                <select name="room" value="<%= booking.roomType %>" class="form-select">
                                    <option value="Standard" <%= booking.room === 'Standard' ? 'selected' : '' %>>Standard</option>
                                    <option value="Deluxe" <%= booking.room === 'Deluxe' ? 'selected' : '' %>>Deluxe</option>
                                    <option value="Suite" <%= booking.room === 'Suite' ? 'selected' : '' %>>Suite</option>
                                    <option value="Penthouse" <%= booking.room === 'Penthouse' ? 'selected' : '' %>>Penthouse</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Check-In Date</label>
                                <input type="date" value="<%= booking.checkInDate %>" name="checkIn" class="form-control" value="<%= booking.checkIn %>" required>
                            </div>
                            <div class="mb-3">
                                <label class="form-label">Check-Out Date</label>
                                <input type="date" value="<%= booking.checkOutDate %>" name="checkOut" class="form-control" value="<%= booking.checkOut %>" required>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Status</label>
                                <select name="status" value="<%= booking.status %>" class="form-select">
                                    <option value="Pending" <%= booking.status === 'Pending' ? 'selected' : '' %>>Pending</option>
                                    <option value="Confirmed" <%= booking.status === 'Confirmed' ? 'selected' : '' %>>Confirmed</option>
                                    <option value="Checked Out" <%= booking.status === 'Checked Out' ? 'selected' : '' %>>Checked Out</option>
                                </select>
                            </div>

                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-primary">Update Booking</button>
                                <a href="/" class="btn btn-secondary">Cancel</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>
</html>